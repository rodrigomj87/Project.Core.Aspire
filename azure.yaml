# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: Project.Core
services:
  app:
    language: dotnet
    project: ./src/Project.Core.AppHost/Project.Core.AppHost.csproj
    host: containerapp
